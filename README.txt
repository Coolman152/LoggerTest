Logger v0.0.1 – Minimal Render Test

This is a stripped-down Three.js scene.
If this does NOT render a green ground and spinning red cube,
your school network is blocking Three.js from loading.

If it DOES render correctly,
then the issue is inside our main game code and we’ll isolate it next.
